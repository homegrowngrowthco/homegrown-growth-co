# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Misc
*.log

# Local Netlify folder
.netlify

# Environment variables
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build artifacts
dist/
.next/
.astro/

# Deploy CLIs
.vercel/

# Claude Code (commit settings.json; ignore local overrides + history)
.claude/settings.local.json
.claude/file-history/

# Reference screenshots + raw brand asset sources kept at repo root
# (processed copies live in public/ where applicable)
*_whitespace.jpg
whitespace.jpg
68dd418739aadd6263219522_clay-logo.png

# GSC tooling secrets (canonical location is ~/.gsc/, outside the repo —
# these lines are a defensive net in case creds ever land in the tree)
client_secret*.json
token.json
*.gsc.json

# Site-audit working set (2026-06-12): untracked by convention, ignored so a
# broad git add can never sweep the report or its screenshot evidence in.
# Resolved 2026-07-18: both now live under the local-only _archive/ folder.
AUDIT.md
_audit-evidence/
_archive/
